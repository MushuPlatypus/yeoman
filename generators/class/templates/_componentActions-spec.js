import { h } from 'preact';
import testSetup from './../../config/test-setup.js';
import { <%= name %><%= nameMod %> } from './<%= name %><%= nameMod %>';
import { expect, assert } from 'chai';

describe('<%= reference %><%= name %><%= nameMod %>', () => {
  it('should write tests');
});
